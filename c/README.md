# Setup

## Installing gdbk-2020

```
pushd /opt/
wget https://github.com/gbdk-2020/gbdk-2020/releases/download/4.5.0/gbdk-linux64.tar.gz
tar -xvzf gbdk-linux64.tar.gz
popd
export PATH=/opt/gbdk/bin:${PATH}
```
